

pub fn day_02() -> usize {
    // A, X = Rock
    // B, Y = Paper
    // C, Z = Scissors
    let wins = vec!["A Y", "B Z", "C X"];
    let losses = vec!["A Z", "B X", "C Y"];
    let contents = crate::get_file_contents("02".into());
}
